# Configuration for self-check of anneal's own specification.
# Suppress only known prose-example false positives from anneal-spec.md and
# captured agent-feedback references to external Host Corpus corpus files.

exclude = ["feedback"]

[handles]
confirmed = ["OQ", "PD", "CR-D", "CR-R", "CR-OQ", "HR-D", "HR-OQ", "MVS", "SP-Q"]

[suppress]

[[suppress.rules]]
code = "E001"
target = "synthesis/v17.md"
