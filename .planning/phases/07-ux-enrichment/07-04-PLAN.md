---
phase: 07-ux-enrichment
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .design/anneal.toml
autonomous: true
requirements:
  - QUALITY-02
must_haves:
  truths:
    - "anneal --root .design/ check exits 0 (no errors)"
    - "anneal.toml in .design/ contains suppress rules for known false positives"
  artifacts:
    - path: ".design/anneal.toml"
      provides: "Config for self-check with suppress rules"
      contains: "[suppress]"
  key_links:
    - from: ".design/anneal.toml"
      to: "src/config.rs"
      via: "SuppressConfig parsed from TOML"
      pattern: "suppress"
---

<objective>
Make `anneal --root .design/ check` pass cleanly (self-check).

Purpose: QUALITY-02 proves anneal can check its own spec without false positives. Depends on CONFIG-01 (suppress) from plan 01.
Output: .design/anneal.toml with appropriate suppress rules
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ux-enrichment/07-RESEARCH.md
@.planning/phases/07-ux-enrichment/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and suppress self-check false positives</name>
  <files>.design/anneal.toml</files>
  <read_first>.design/anneal-spec.md, src/config.rs</read_first>
  <action>
    1. Run `anneal --root .design/ check 2>&1` to see current diagnostic output. Capture all errors and warnings.

    2. Analyze each diagnostic:
    - If it's a genuine issue in the spec (actual broken reference), fix the spec text.
    - If it's a false positive (prose example, inline code example, etc.), add a suppress rule.

    The research identified that `synthesis/v17.md` is a known false positive -- it appears as a prose reference in the spec (an example of versioned artifacts), not an actual file reference.

    3. Create `.design/anneal.toml` with appropriate suppress rules. Start with:

    ```toml
    # Configuration for self-check of anneal's own specification.
    # Suppress known false positives from prose examples in anneal-spec.md.

    [suppress]
    # Add global code suppressions if needed
    # codes = []

    # Add specific suppressions discovered from the diagnostic output
    # [[suppress.rules]]
    # code = "E001"
    # target = "synthesis/v17.md"
    ```

    The exact rules depend on what `anneal --root .design/ check` reports. Run it, read each E001/W001/etc., determine if each is a false positive from prose, and add targeted suppress rules.

    4. Iterate: run `anneal --root .design/ check` again after creating the config. If errors remain, add more rules or fix spec text. Repeat until exit code is 0.

    5. IMPORTANT: Do NOT suppress real issues. Only suppress references that appear in prose examples (like the spec discussing "formal-model/v17.md" as an example of versioned artifacts). The spec is documentation, not a knowledge corpus -- inline examples should not produce diagnostics.

    6. If there are warnings (W001, W003) or info (I001, I002) diagnostics, those are acceptable -- only errors (E001, E002) cause exit code 1. Suppress errors only; leave warnings for informational value.
  </action>
  <verify>
    <automated>cd /Users/morgan/code/anneal && anneal --root .design/ check 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - .design/anneal.toml exists and contains `[suppress]`
    - `anneal --root .design/ check` exits with code 0
    - Only prose-example false positives are suppressed (not real broken references)
  </acceptance_criteria>
  <done>anneal --root .design/ check passes cleanly with targeted false positive suppression</done>
</task>

</tasks>

<verification>
- `anneal --root .design/ check` exits 0
- `.design/anneal.toml` exists with suppress rules
- `just check` still passes (config changes don't affect tests)
</verification>

<success_criteria>
- QUALITY-02: `anneal --root .design/ check` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-enrichment/07-04-SUMMARY.md`
</output>
