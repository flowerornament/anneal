name: CI

on:
  push:
    branches: [master]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.94.0
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --check

      - name: Installer
        run: |
          bash -n install.sh
          bash install.sh --help >/dev/null
          bash install.sh --print-target >/dev/null
          bash install.sh --dry-run >/dev/null

      - name: Installer smoke test
        run: |
          # Pin to the version in Cargo.toml instead of "latest" — the
          # Release workflow runs in parallel with CI on a tag push, so
          # "latest" races and fetches the previous release's SKILL.md.
          # Wait for the matching release to publish. The macOS release build is
          # often the long pole, so this must be comfortably longer than the
          # release workflow's normal build window.
          version=$(grep '^version = ' Cargo.toml | head -1 | cut -d'"' -f2)
          tag="v$version"
          repo="${{ github.repository }}"

          for attempt in $(seq 1 120); do
            if curl -sf -o /dev/null "https://api.github.com/repos/$repo/releases/tags/$tag"; then
              break
            fi
            if [ "$attempt" -eq 120 ]; then
              echo "::error::release $tag not published after 600s — Release workflow may have failed"
              exit 1
            fi
            echo "waiting for release $tag (attempt $attempt/120)..."
            sleep 5
          done

          tmpdir="$(mktemp -d)"
          INSTALL_DIR="$tmpdir/bin" bash install.sh --tag "$tag" --skill-target "$tmpdir/skills/anneal"
          "$tmpdir/bin/anneal" --version
          diff -ru skills/anneal "$tmpdir/skills/anneal"

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Test
        env:
          # Force console-crate ANSI emission so attribute leaks on
          # color-disabled paths can't be silently neutered by GitHub
          # Actions' non-TTY detection. This mirrors what Nix build
          # sandboxes see and caught once.
          CLICOLOR_FORCE: 1
        run: cargo test

      - name: Build
        run: cargo build --release

  nix-home-manager-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Home Manager module smoke test
        run: bash scripts/test-home-manager-module.sh
